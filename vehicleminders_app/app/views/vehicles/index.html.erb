<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="UTF-8">
    <title>CSVをインポートする</title>
  </head>
  <body>
  <table>
  <tr>
    <th>メーカー</th>
    <th>モデル</th>
    <th>製造年</th>
    <th>ナンバープレート</th>
    <th>リース満了日</th>
    <th>車検更新日</th>
  </tr>
  <% @vehicles.each do |vehicle| %>
    <tr>
      <td><%= vehicle.maker %></td>
      <td><%= vehicle.model %></td>
      <td><%= vehicle.production_year %></td>
      <td><%= vehicle.license_plate %></td>
      <td><%= vehicle.lease_expiry %></td>
      <td><%= vehicle.inspection_due %></td>
      <td><%= link_to "編集", "/vehicles/#{vehicle.id}/edit" %></td>
      <td><%= link_to "削除", "/vehicles/#{vehicle.id}", data: {turbo_method: :delete, turbo_confirm: '本当に削除しますか?'} %></td>
    </tr>
  <% end %>
</table>

  </body>
</html>