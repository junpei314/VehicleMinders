<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="UTF-8">
    <title>CSVをインポートする</title>
  </head>
  <body>

  <%= form_with model:@notification ,local: true do |form| %>
  <table class="edit-table">
    <tr>
      <th>通知日</th>
      <th>通知時間</th>
    </tr>
    <tr class="cell">
      <td><%= form.date_field :date, placeholder: @date.strftime("%Y-%m-%d") %></td>
      <td><%= form.time_field :time, placeholder: @time %></td>
      <td><%= form.submit "更新" %></td>
    </tr>
  </table>
<% end %>

  </body>
</html>